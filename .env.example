GOOGLE_GENAI_USE_ENTERPRISE=0

# Use "google" for Gemini API, "nvidia" for NVIDIA NIM, or "local" for Ollama/LiteLLM.
MODEL_PROVIDER=google

# Use a Gemini API key from Google AI Studio.
# The SDK accepts GEMINI_API_KEY or GOOGLE_API_KEY.
# If both are set, GOOGLE_API_KEY takes precedence.
GOOGLE_API_KEY=your-google-ai-studio-api-key
GOOGLE_MODEL=gemini-flash-latest

# Local model settings for MODEL_PROVIDER=local or the "ADK Web (Ollama)" launch profile.
LOCAL_MODEL=ollama_chat/qwen3:4b

# NVIDIA NIM settings for MODEL_PROVIDER=nvidia or the "ADK Web (NVIDIA)" launch profile.
NVIDIA_NIM_API_KEY=REPLACE_WITH_YOUR_NVIDIA_NIM_API_KEY
NVIDIA_MODEL=nvidia_nim/deepseek-ai/deepseek-v4-flash

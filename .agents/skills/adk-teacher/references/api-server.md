# API Server

Use this reference when the conversation is about exposing an ADK agent
through a REST API.

## Core idea

The API server lets you run the agent behind a service interface instead of
only through interactive local tools.

## Why it matters

It is the bridge from local development to service-style access.

## Source note

Official URLs:
- https://adk.dev/runtime/api-server/
